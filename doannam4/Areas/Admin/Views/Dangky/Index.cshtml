@using doannam4.Models;

@model IList<Dangky>
@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<main id="main" class="main">
    <div class="pagetitle">
        <h2>Danh sách email đăng ký nhận thông báo bài viết mới</h2>
    </div>
    <p>
    </p>
    <section class="section dashboard">
        <div class="row">
            <div class="col-12">
                <div class="card recent-sales overflow-auto">
                    <div class="card-body mt-4">
                        <table class="table table-borderless datatable">
                            <thead>
                                <tr>
                                    <th class="col-1 text-center">#</th>

                                    <th class="col-2 text-center">Email</th>

                                    <th class="col-2 text-center">Function</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <th class="text-center" scope="row">@item.DkyID</th>

                                        <td class="text-center">
                                            <a asp-area="Admin" asp-controller="Dangky" asp-action="Details"
                                           asp-route-id="@item.DkyID" class="text-primary">@item.Emaildky</a>
                                        </td>

                                        <td class="text-center">
                                            <a asp-area="Admin" asp-controller="Dangky" asp-action="Delete"
                                           asp-route-id="@item.DkyID" class="btn btn-danger btn-sm"
                                           title="Xoá email đăng ký"><i class="bi bi-trash"></i></a>
                                        </td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>
